Command: mkdir IOInvestigation
Explanation:
The mkdir command created a new directory named IOInvestigation. This directory was used for the file I/O investigation.

Command: cd IOInvestigation
Explanation:
The cd command changed the current working directory to IOInvestigation.

Command: echo "Linux File I/O Investigation" > sample.txt
Explanation:
The echo command created a file named sample.txt and stored sample text inside it.

Command: lsof sample.txt
Explanation:
The lsof command checked whether the sample.txt file was open by any running process. No process was using the file at the time of execution.

Command: ls -l > output.txt
Explanation:
The output of the ls -l command was redirected to output.txt using output redirection. This stored the directory listing in a file instead of displaying it on the screen.

Command: cat output.txt
Explanation:
The cat command displayed the contents of output.txt. It confirmed that the output redirection worked successfully.

Command: ls invalidfile 2> error.txt
Explanation:
The error message generated by the invalid command was redirected to error.txt using standard error redirection.

Command: cat error.txt
Explanation:
The cat command displayed the contents of error.txt. It confirmed that the error message was captured successfully.

Command: ulimit -a
Explanation:
The ulimit -a command displayed the current system resource limits for the user, including file size, memory, and open file limits.

Command: cat IO_Investigation_Report.txt
Explanation:
The cat command displayed the contents of the IO_Investigation_Report.txt file. It verified that the report was created successfully.